<nav class="nav container">
      <img id='mobileLogo' src='https://i.ibb.co/cNqdNvt/logo-click-and-trip-blanc-petit.png' alt='logo click & Trip' a/>
    <div id="mainListDiv" class="main_list">
        <ul class="navlinks">
            <li><a href="{{ path('homepage') }}">Accueil</a></li>
            <li>
                <div class="dropdown">
                    <a href="#" id="dropdown" class="dropbtn">Mon compte</a>
                    <div id="myDropdown" class="dropdown-content">
                        {% if app.user %}
                        <a href="{{ path('profil_mailbox') }}">messages</a>
                        <a href="{{ path('profil_history') }}">historiques</a>
                        {% if instanceofClient(app.user) %}
                            <a href="{{ path('profil_edit_client') }}"> <option value="Info perso">Info perso </option> </a>
                        {% endif %}
                        {% if instanceofAgency(app.user) %}
                            <a href="{{ path('profil_edit_agency') }}"> <option value="Info perso">Info perso </option> </a>
                        {% endif %}
                        <a href="{{ path('compte_deconnexion') }}">déconnexion</a>
                    </div>
                    {% else %}
                        <a href="{{ path('compte_inscription') }}">Inscription</a>
                        <a href="{{ path('compte_connexion') }}">Connexion</a>
                    {% endif %}
                </div>
            </li>
            <li><a href="{{ path('about_inspiration') }}">Inspirations</a></li>
            <li  id='laptopLogo'><a href="{{ path('homepage') }}"> <img id="logo" src='https://i.ibb.co/cNqdNvt/logo-click-and-trip-blanc-petit.png' alt='logo click & Trip'/></a></li>
            <li><a href="{{ path('planner') }}">Créer Mon Voyage</a></li>
            <li>
                <div class="dropdown">
                    <a href="#" id="dropdown2" class="dropbtn"  onclick="getCountries()">Destinations</a>
                    <div id="myDropdown2" class="dropdown-content">
                    </div>
                </div>
            </li>
            <li><a href="{{ path('about_contact') }}">Contact</a></li>
        </ul>
    </div>
    <span class="navTrigger">
      <i></i>
      <i></i>
      <i></i>
  </span>
</nav>
<script>
    function getCountries() {
        fetch('http://localhost:8000/api/stages')
            .then(results => results.json())
            .then(data => {
                let dest = data.map(stage => stage.destination);
                let unique = [...new Set(dest)];
                const dropdown = document.getElementById('myDropdown2');
                let html = '';
                unique.forEach(country =>
                    html += `<a href='/mon-voyage/${country}'> ${country} </a>`);
                dropdown.innerHTML = html;

            })
    }
</script>