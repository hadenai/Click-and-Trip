{{ form_start(form) }}
    {# {{ form_widget(form) }} #}
    {# {{ form_row(form.prices) }} #}
    <ul class="prices" data-prototype="{{ form_widget(form.prices.vars.prototype)|e('html_attr') }}"></ul>
    <button class="btn">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
