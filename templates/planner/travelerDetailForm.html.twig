{% extends "base.html.twig" %}
    {% block stylesheet %}
        {{ encore_entry_link_tags('travelerDetailForm') }}
    {% endblock %}

{% block body %}

    <div id="padding-for-navbar">
        <div class="formContainer">
        <h1>Commencez à créer votre projet voyage...</h1>
        {{ form_start(form, {
            'attr': {
                'class' : 'ui form formulaire'
            }
        }) }}
        {{ form_errors(form) }}

        <h3>Combien de participants ?</h3>

        <p>Merci de fournir le nombre de participant et l’âge des voyageurs qui aide notre expert local à personnaliser votre voyage.</p>

        <h4>Adultes:</h4>

        <div class="ui input focus">
            {{ form_row(form.adulte1, {'value' : 0}) }}
            {{ form_row(form.adulte2, {'value' : 0}) }}
            {{ form_row(form.adulte3, {'value' : 0}) }}
            {{ form_row(form.adulte4, {'value' : 0}) }}
        </div>

        <h4>Enfants</h4>

        <div class="ui input focus">
            {{ form_row(form.enfants1, {'value' : 0}) }}
            {{ form_row(form.enfants2, {'value' : 0}) }}
            {{ form_row(form.enfants3, {'value' : 0}) }}
            {{ form_row(form.enfants4, {'value' : 0}) }}
        </div>

        <h2>Quand souhaitez-vous partir ?</h2>
        <h5>Avez-vous une idée précise de vos dates de voyages ?</h5>
        {# <div class="ui buttons">
            <button class="ui button" >oui</button>
            <div class="or" data-text="ou"></div>
            <button class="ui positive button">non</button>
        </div> #}
        {# <div class="blue ui buttons">
            <button class="ui button" onclick="() => handleDate(dateAccurated, dateLarge)">Oui</button>
            <button class="ui button" onclick="() => handleDate(dateLarge, dateAccurated)">Un peu</button>
            <button class="ui button">Non</button>
        </div> #}
        <div class="ui three item menu">
            <a class="item" onclick="handleDate(dateAccurated, dateLarge)">Oui</a>
            <a class="item" onclick="handleDate(dateAccurated, dateLarge)">Un peu</a>
            <a class="item">Non</a>
        </div>

        <div class="field" id="dateAccurated">
            {# <div class="ui checkbox">
                {{ form_row(form.travelDate1) }}
            </div> #}
            {{ form_row(form.beginDate, {
                'attr': {
                    'class': 'fields'
                }
            }) }}
            {{ form_row(form.endDate, {
                'attr': {
                    'class': 'fields'
                }
            }) }}
        </div>

        <div class="field" id="dateLarge">
            {# <div class="ui checkbox">
                {{ form_row(form.travelDate2) }}
            </div> #}
                {{ form_row(form.mois) }}
                {{ form_row(form.annees) }}
        </div>

        {# <div class="fields">
            <div class="field">
            </div>
            <div class="field">
            </div>
        </div> #}

        {# <div class="field">
            <div class="ui checkbox">
                {{ form_row(form.travelDate3) }}
            </div>
        </div> #}

        <h2>Quelles sont vos disponibilités pour un rendez-vous téléphonique?</h2>

        <div class="field">
            <div class="ui checkbox">
                {{ form_row(form.disponibilityDays) }}
            </div>
            <div class="ui checkbox">
                {{ form_row(form.disponibilityHour) }}
            </div>
        </div>
        
        {{ form_row(form.phoneNumber) }}

            <p id="details">
                Vous avez presque terminé ! Veuillez nous en dire le plus possible sur vos projets et vos exigences particulières.
                Plus il y a de détails, plus votre expert local pourra personnaliser votre voyage !
                Merci de nous préciser tout lieu ou expérience que vous souhaitez absolument inclure dans votre itinéraire.
                Pour commencer à planifier votre voyage, vous devez disposer d'un compte Click&Trip. Avecvotre adresse e-mail et un mot de passe,
                vous pourrez planifier votre voyage depuis n'importe quel téléphone ou ordinateur.
            </p>

            <div class="field">
                {{ form_widget(form.comment, { 'attr': {
                    'rows' : '15',
                    'cols' : '35' 
                    }})
                }}
            </div>

            <div class="field">
                <div class="ui checkbox">
                    {# {{ form_row(form.cgu) }} #}
                    {{ form_label(form.cgu) }}
                    {{ form_widget(form.cgu) }}
                </div>
            </div>

            <button type="submit" class="ui secondary button">J'envoie ma demande</button>
            {{ form_end(form) }}
        </div>
    </div>

    

{% endblock %}

{% block javascripts %}
    {{ encore_entry_script_tags('app') }}
    <script src="//ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <script>
        function handleDate(showById, hideById){
            const SHOW = document.getElementById(`${showById}`);
            SHOW.style.display="block";
            document.getElementById(`${hideById}`).style.display="none";
            document.getElementsByClassName('active')[0].classList.remove('active');
            SHOW.classList.add('active');
        }

        $('.ui.checkbox').checkbox();
    </script>
{% endblock %}
